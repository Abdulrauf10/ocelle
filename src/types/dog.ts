type BreedSize = 'Small' | 'Medium' | 'Large';

type Gender = 'M' | 'F';

type DateOfBirthMethod = 'Manually' | 'Calendar';

type BodyCondition = 'TooSkinny' | 'JustRight' | 'Rounded' | 'Chunky';

type ActivityLevel = 'Mellow' | 'Active' | 'VeryActive';

type CurrentlyEating = 'Dry' | 'Wet' | 'Raw' | 'Dehydrated' | 'Fresh' | 'Homemade' | 'Other';

type AmountOfTreats = 'None' | 'Some' | 'Lots';

type Pickiness = 'Picky' | 'GoodEater' | 'EatAnything';

type LifeStage = 'Puppy' | 'Adult' | 'Senior';

export type {
  BreedSize,
  Gender,
  DateOfBirthMethod,
  BodyCondition,
  ActivityLevel,
  CurrentlyEating,
  AmountOfTreats,
  Pickiness,
  LifeStage,
};
