mutation UserUpdate($id: ID!, $firstName: String!, $lastName: String!, $email: String!) {
  customerUpdate(id: $id, input: { firstName: $firstName, lastName: $lastName, email: $email }) {
    errors {
      ...UserError
    }
  }
}
